{
  "name": "typescript-monorepo",
  "version": "1.0.0",
  "description": "TypeScript full-stack monorepo example",
  "scripts": {
    "start": "(cd packages/playground && npm start)",
    "test": "echo \"Error: no test specified\" && exit 1",
    "bootstrap": "lerna bootstrap && npm run build",
    "build": "tsc --build packages",
    "clean": "tsc --build packages --clean && lerna run clean"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.15.0",
    "typescript": "^3.5.2"
  }
}
